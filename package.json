{
  "name": "teamgrid-crm",
  "version": "1.0.0",
  "description": "Teamgrid CRM — monorepo root",
  "scripts": {
    "start": "cd server && node server.js",
    "build": "cd server && npm install",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
